parameters:
  charts:
    gitlab:
      helm_values: 
        global:
          hosts:
            domain: ${fqdn}
            gitlab:
              name: ${fqdn}
          pages:
            host: pages.${fqdn}
