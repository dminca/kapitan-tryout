classes:
  - common
  - gitlab.helm

parameters:
  fqdn: stg.example.com
  charts:
    gitlab:
      helm_values:
        global:
          hosts:
            domain: ${fqdn}
            gitlab:
              name: gitlab.${fqdn}
